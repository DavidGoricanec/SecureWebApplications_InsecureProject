#!/bin/bash
echo "Startup the server"
echo ""

echo "Note, after we start the server you might get resources with 'curl localhost:'"
echo "Starting the JavaScript server (needs the compiled JS in ./dist folder) with 'npm start'"
npm start
